#!/bin/sh
# XDM invokes this after PAM authentication, as the authenticated user.
set -eu
[ "$(id -u)" -ne 0 ] || exit 1
umask 077
cd "$HOME"
export WIIDESK_MANAGED_SESSION=1
export WIIDESK_LOCKER=/usr/bin/xsecurelock
exec /usr/local/lib/wiidesk-session/wiidesk-x11
